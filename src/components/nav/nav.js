import React,{useState} from 'react';
import './nav.css';

const Nav = () => {
    
    return (
        <header className="headerBar">  
            <a href="#">HOME</a>
            <a href="#">ABOUT ME</a>
        </header>
    );
};

export default Nav